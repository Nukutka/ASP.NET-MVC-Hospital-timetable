@model IEnumerable<Test.Models.TimeTable>

<div class="col-md-9">
    <div class="row">
        <h3>Расписание специалистов</h3>
        @{ foreach (var timeTable in Model)
            {
                <div class="col-md-2">
                    @{Html.RenderPartial("TTColumn", timeTable);}
                </div>
            }
        }
    </div>
</div>
